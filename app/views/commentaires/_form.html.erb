<%= form_for([@article, @article.commentaires.build]) do |f| %>
    <p>
        <%= f.label :auteur %><br>
    	<%= f.text_field :auteur %>
    </p>
    <p>
    	<%= f.label :corp , 'Commentaire'%><br>
    	<%= f.text_area :corp %>
    </p>
    <p>
    	<%= f.submit %>
    </p>
<% end %>